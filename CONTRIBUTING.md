# 贡献指南

欢迎改进方法规则、文档、校验逻辑和效果案例。

## 提交问题

请提供脚本命令、Python 版本、错误信息和最小复现输入。提交导演方案前，删除私人素材地址、未公开稿件和凭证。视觉问题应区分「策划缺陷」与「模型没有按方案执行」。

## 修改与检查

1. 保持改动聚焦，说明影响的信号、规则或输出字段。
2. 修改结构约定时，同步检查 Schema、模板与校验器。
3. 修改触发矩阵后，运行 `python scripts/build_method_runtime_contract.py` 重建运行时合同，不直接手改生成的 JSON。
4. 按 README 的开发步骤运行自测和合同一致性检查。影响路由时，补跑 `--exhaustive-test`。
5. 在 Pull Request 中列出验证结果与尚未验证的部分。

请勿把校验器通过描述为生成视频质量通过。贡献按本仓库的 MIT License 提交；第三方材料应有明确的再分发授权，不得仅因可在线观看就上传副本。

## 效果案例

按 [examples/README.md](examples/README.md) 的约定提供输入、方案、视频和复核说明。不得上传 API Key、临时签名链接、客户资料或未获授权的人像、音乐与品牌素材。
